import 'package:flutter/material.dart';

class CustomButton extends StatelessWidget {
  final String? label;
  final Color? color;
  final dynamic? icon;
  const CustomButton({super.key, this.label, this.color, this.icon});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}